# Backlog

- Integrate real Google OAuth for practice accounts (currently UI-only button on `index.html#sign-in`).
- Integrate real Apple sign-in for practice accounts (currently UI-only button on `index.html#sign-in`).
- Implement "Forgot password" flow for practice accounts (`/api/auth/forgot` + reset token flow).
- Replace placeholder Terms/Privacy links with real policy pages.
- Add explicit admin sign-out button within the Admin & CMS panel (currently admin sign-out is session-expiry/manual refresh based).
