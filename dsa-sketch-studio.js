/**
 * DSA problem sketch — Sketch Studio Pro UI (embedded).
 * Generated by scripts/build-sketch-studio.py — edit that script or _sketch_extract_* then re-run.
 */
function dsaWireSketchEditorStudio(editorRoot, onChange, sketchOpts) {
    const hooks = sketchOpts || {};
    if (!editorRoot) {
        return dsaWireSketchEditorStudioStub();
    }
    if (!document.head.querySelector("link[data-dsa-sketch-studio-css]")) {
        const lk = document.createElement("link");
        lk.rel = "stylesheet";
        lk.href = "./dsa-sketch-studio.css?v=1";
        lk.dataset.dsaSketchStudioCss = "1";
        document.head.appendChild(lk);
    }
    editorRoot.innerHTML = "";
    editorRoot.classList.add("dsa-sketch-studio-host");
    const mount = document.createElement("div");
    mount.className = "dsa-sketch-studio-mount";
    mount.dataset.theme = "light";
    mount.innerHTML = "<div class=\"app\" id=\"dsaSkApp\">\n<header class=\"header\">\n<div class=\"brand\">\n<div class=\"logo\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M12 19l7-7 3 3-7 7-3-3z\"/><path d=\"M18 13l-1.5-7.5L2 2l3.5 14.5L13 18l5-5z\"/><circle cx=\"11\" cy=\"11\" r=\"2\"/></svg></div>\n<div class=\"brand-text\"><h1>Sketch Studio</h1><p>Pro • Smooth Retina</p></div>\n</div>\n<div class=\"header-actions\">\n<button type=\"button\" class=\"btn-flat dsa-sketch-fs-back\" id=\"dsaSkBackBtn\" data-tip=\"Back to dialog\">Back</button>\n<button class=\"sw\" id=\"dsaSkThemeBtn\" data-tip=\"Theme\"></button>\n<label class=\"btn-flat\" data-tip=\"Import\" style=\"cursor:pointer\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><rect x=\"3\" y=\"3\" width=\"18\" height=\"18\" rx=\"2\"/><circle cx=\"8.5\" cy=\"8.5\" r=\"1.5\"/><polyline points=\"21 15 16 10 5 21\"/></svg>Import<input type=\"file\" id=\"dsaSkImgInput\" accept=\"image/*\" hidden/></label>\n<button class=\"btn-flat primary\" id=\"dsaSkSaveBtn\" data-tip=\"Save PNG\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4\"/><polyline points=\"7 10 12 15 17 10\"/><line x1=\"12\" y1=\"15\" x2=\"12\" y2=\"3\"/></svg>Save</button>\n<button class=\"btn-flat\" id=\"dsaSkClearBtn\" data-tip=\"Clear\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><polyline points=\"3 6 5 6 21 6\"/><path d=\"M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6\"/></svg></button>\n</div>\n</header>\n<div class=\"toolbar\">\n<div class=\"group\">\n<button class=\"btn\" id=\"dsaSkFsBtn\" data-tip=\"Fullscreen\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M3 8V5a2 2 0 0 1 2-2h3M21 8V5a2 2 0 0 0-2-2h-3M3 16v3a2 2 0 0 0 2 2h3M21 16v3a2 2 0 0 1-2 2h-3\"/></svg></button>\n<button class=\"btn\" id=\"dsaSkZiBtn\" data-tip=\"Zoom In\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><circle cx=\"11\" cy=\"11\" r=\"7\"/><line x1=\"21\" y1=\"21\" x2=\"16.65\" y2=\"16.65\"/><line x1=\"11\" y1=\"8\" x2=\"11\" y2=\"14\"/><line x1=\"8\" y1=\"11\" x2=\"14\" y2=\"11\"/></svg></button>\n<button class=\"btn\" id=\"dsaSkZoBtn\" data-tip=\"Zoom Out\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><circle cx=\"11\" cy=\"11\" r=\"7\"/><line x1=\"21\" y1=\"21\" x2=\"16.65\" y2=\"16.65\"/><line x1=\"8\" y1=\"11\" x2=\"14\" y2=\"11\"/></svg></button>\n<button class=\"btn\" id=\"dsaSkZrBtn\" data-tip=\"Reset\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M3 12a9 9 0 0 1 15-6.7L21 8\"/><path d=\"M21 3v5h-5\"/><path d=\"M21 12a9 9 0 0 1-15 6.7L3 16\"/><path d=\"M3 21v-5h5\"/></svg></button>\n</div>\n<div class=\"group\">\n<button class=\"btn\" id=\"dsaSkUndoBtn\" data-tip=\"Undo\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M3 7v6h6\"/><path d=\"M21 17a9 9 0 0 0-15-6.7L3 13\"/></svg></button>\n<button class=\"btn\" id=\"dsaSkRedoBtn\" data-tip=\"Redo\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M21 7v6h-6\"/><path d=\"M3 17a9 9 0 0 1 15-6.7L21 13\"/></svg></button>\n</div>\n<div class=\"group\">\n<button class=\"btn active\" data-tool=\"brush\" data-tip=\"Brush\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M12 19l7-7 3 3-7 7-3-3z\"/><path d=\"M18 13l-1.5-7.5L2 2l3.5 14.5L13 18l5-5z\"/></svg></button>\n<button class=\"btn\" data-tool=\"pencil\" data-tip=\"Pencil\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M12 20h9\"/><path d=\"M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z\"/></svg></button>\n<button class=\"btn\" data-tool=\"marker\" data-tip=\"Highlighter\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M9 11l-6 6v3h3l6-6\"/><path d=\"M22 12l-4.6 4.6a2 2 0 0 1-2.8 0L9 11l5-5 8 6z\"/></svg></button>\n<button class=\"btn\" data-tool=\"eraser\" data-tip=\"Eraser\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M20 20H7l-4-4 11-11 8 8-7 7\"/><line x1=\"14\" y1=\"6\" x2=\"20\" y2=\"12\"/></svg></button>\n<button class=\"btn\" data-tool=\"laser\" data-tip=\"Laser Torch\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M18 6c0-1.1.9-2 2-2s2 .9 2 2v.5L10 18.5V20a2 2 0 0 1-2 2H6c-1.1 0-2-.9-2-2v-2c0-1.1.9-2 2-2h2v-2L18 6.5V6z\"/></svg></button>\n</div>\n<div class=\"group\">\n<button class=\"btn\" data-tool=\"line\" data-tip=\"Line\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\"><line x1=\"4\" y1=\"20\" x2=\"20\" y2=\"4\"/></svg></button>\n<button class=\"btn\" data-tool=\"rect\" data-tip=\"Rectangle\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><rect x=\"4\" y=\"4\" width=\"16\" height=\"16\" rx=\"2\"/></svg></button>\n<button class=\"btn\" data-tool=\"circle\" data-tip=\"Circle\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><circle cx=\"12\" cy=\"12\" r=\"9\"/></svg></button>\n<button class=\"btn\" data-tool=\"arrow\" data-tip=\"Arrow\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><line x1=\"4\" y1=\"20\" x2=\"20\" y2=\"4\"/><polyline points=\"10 4 20 4 20 14\"/></svg></button>\n<button class=\"btn\" data-tool=\"text\" data-tip=\"Text\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\"><polyline points=\"4 7 4 4 20 4 20 7\"/><line x1=\"9\" y1=\"20\" x2=\"15\" y2=\"20\"/><line x1=\"12\" y1=\"4\" x2=\"12\" y2=\"20\"/></svg></button>\n</div>\n<div class=\"sg\"><label>Size</label><input type=\"range\" id=\"dsaSkSizeR\" min=\"1\" max=\"80\" value=\"6\"/><span class=\"chip\" id=\"dsaSkSizeV\">6</span></div>\n<div class=\"sg\"><label>Opacity</label><input type=\"range\" id=\"dsaSkOpR\" min=\"10\" max=\"100\" value=\"100\"/><span class=\"chip\" id=\"dsaSkOpV\">100</span></div>\n<div class=\"colors\" id=\"dsaSkColors\">\n<div class=\"swatch active\" data-color=\"#1d1d1f\" style=\"background:#1d1d1f\"></div>\n<div class=\"swatch\" data-color=\"#ffffff\" style=\"background:#fff\"></div>\n<div class=\"swatch\" data-color=\"#ff3b30\" style=\"background:#ff3b30\"></div>\n<div class=\"swatch\" data-color=\"#ff9500\" style=\"background:#ff9500\"></div>\n<div class=\"swatch\" data-color=\"#ffcc00\" style=\"background:#ffcc00\"></div>\n<div class=\"swatch\" data-color=\"#34c759\" style=\"background:#34c759\"></div>\n<div class=\"swatch\" data-color=\"#007aff\" style=\"background:#007aff\"></div>\n<div class=\"swatch\" data-color=\"#5856d6\" style=\"background:#5856d6\"></div>\n<div class=\"swatch\" data-color=\"#af52de\" style=\"background:#af52de\"></div>\n<div class=\"swatch\" data-color=\"#ff2d55\" style=\"background:#ff2d55\"></div>\n<label class=\"cpw\" data-tip=\"Custom\"><input type=\"color\" id=\"dsaSkCp\" value=\"#000000\"/></label>\n</div>\n</div>\n<div class=\"cw\" id=\"dsaSkCw\">\n<canvas id=\"dsaSkCv\"></canvas>\n<canvas id=\"dsaSkOv\"></canvas>\n<div class=\"fz\"><svg width=\"13\" height=\"13\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2.5\"><circle cx=\"11\" cy=\"11\" r=\"7\"/><line x1=\"21\" y1=\"21\" x2=\"16.65\" y2=\"16.65\"/></svg><span id=\"dsaSkZV\">100%</span></div>\n</div>\n<div class=\"status\">\n<div class=\"srow\"><span>Tool · <span class=\"pill\" id=\"dsaSkST\">Brush</span></span><span>Color · <span class=\"pill\"><span class=\"sdot\" id=\"dsaSkSCD\" style=\"background:#1d1d1f\"></span><span id=\"dsaSkSCT\">#1d1d1f</span></span></span><span>Size · <span class=\"pill\" id=\"dsaSkSS\">6 px</span></span></div>\n<div class=\"srow\"><span class=\"pill\" id=\"dsaSkSP\">x: 0 · y: 0</span></div>\n</div>\n</div>\n<div class=\"toast\" id=\"dsaSkToast\"><span id=\"dsaSkTT\"></span></div>";
    editorRoot.appendChild(mount);

    let hasInk = false;
    let destroyed = false;

function $(id){return mount.querySelector('#'+id);}

const cv=$('dsaSkCv'),ctx=cv.getContext('2d');
const ov=$('dsaSkOv'),octx=ov.getContext('2d');
const cw=$('dsaSkCw'),app=$('dsaSkApp');
const DPR=Math.max(window.devicePixelRatio||1,1);
const S={tool:'brush',color:'#1d1d1f',size:6,opacity:1,drawing:false,sx:0,sy:0,snap:null,hist:[],redo:[],zoom:1,pts:[],laser:[],laserActive:false,laserStop:0};
const FADE=900;
function roBool(){return editorRoot.classList.contains('dsa-sketch-studio-host--ro');}
function syncInk(){
  hasInk=false;
  try{
    const d=ctx.getImageData(0,0,cv.width,cv.height).data;
    for(let i=0;i<d.length;i+=16){
      if(d[i+3]<8)continue;
      if(d[i]<252||d[i+1]<252||d[i+2]<252){hasInk=true;return;}
    }
  }catch(_){hasInk=S.hist.length>1;}
}
let isFakeFs=false,fsParent=null,fsNext=null,fsHideBackdrop=null,fsHideDlg=null;
function enterFakeFullscreen(){
  if(isFakeFs)return;
  fsParent=editorRoot.parentNode;fsNext=editorRoot.nextSibling;
  fsHideBackdrop=editorRoot.closest('.dsa-dialog-backdrop');
  fsHideDlg=editorRoot.closest('.dsa-dialog');
  document.body.appendChild(editorRoot);
  editorRoot.classList.add('dsa-sketch-studio-host--fullscreen');
  if(fsHideBackdrop)fsHideBackdrop.classList.add('dsa-sketch-fs-hide-dialog');
  if(fsHideDlg)fsHideDlg.classList.add('dsa-sketch-fs-hide-dialog');
  isFakeFs=true;
  requestAnimationFrame(()=>setTimeout(resize,80));
}
function leaveFakeFullscreen(){
  if(!isFakeFs)return;
  if(fsHideBackdrop)fsHideBackdrop.classList.remove('dsa-sketch-fs-hide-dialog');
  if(fsHideDlg)fsHideDlg.classList.remove('dsa-sketch-fs-hide-dialog');
  fsHideBackdrop=null;fsHideDlg=null;
  editorRoot.classList.remove('dsa-sketch-studio-host--fullscreen');
  if(fsParent){
    if(fsNext)fsParent.insertBefore(editorRoot,fsNext);
    else fsParent.appendChild(editorRoot);
  }
  fsParent=null;fsNext=null;isFakeFs=false;
  requestAnimationFrame(()=>setTimeout(resize,80));
}
const isDark=()=>mount.dataset.theme==='dark';
function resize(){
  const r=cw.getBoundingClientRect(),w=Math.floor(r.width),h=Math.floor(r.height);
  let tmp=null;
  if(cv.width&&cv.height){tmp=document.createElement('canvas');tmp.width=cv.width;tmp.height=cv.height;tmp.getContext('2d').drawImage(cv,0,0);}
  cv.width=w*DPR;cv.height=h*DPR;ov.width=w*DPR;ov.height=h*DPR;
  cv.style.width=w+'px';cv.style.height=h+'px';ov.style.width=w+'px';ov.style.height=h+'px';
  ctx.setTransform(DPR,0,0,DPR,0,0);octx.setTransform(DPR,0,0,DPR,0,0);
  ctx.imageSmoothingEnabled=true;ctx.imageSmoothingQuality='high';
  octx.imageSmoothingEnabled=true;octx.imageSmoothingQuality='high';
  if(tmp){ctx.save();ctx.setTransform(1,0,0,1,0,0);ctx.drawImage(tmp,0,0,cv.width,cv.height);ctx.restore();}
  applyZoom();
}
function applyZoom(){cv.style.transform=`scale(${S.zoom})`;ov.style.transform=`scale(${S.zoom})`;$('dsaSkZV').textContent=Math.round(S.zoom*100)+'%';}
function saveH(){try{S.hist.push(cv.toDataURL());if(S.hist.length>60)S.hist.shift();S.redo=[];}catch(e){}}
function saveHNotify(){saveH();syncInk();onChange();}
function restore(d){const i=new Image();i.onload=()=>{ctx.save();ctx.setTransform(1,0,0,1,0,0);ctx.clearRect(0,0,cv.width,cv.height);ctx.drawImage(i,0,0,cv.width,cv.height);ctx.restore();syncInk();onChange();};i.src=d;}
function undo(){if(S.hist.length>1){S.redo.push(S.hist.pop());restore(S.hist[S.hist.length-1]);toast('Undo');}}
function redoFn(){if(S.redo.length){const d=S.redo.pop();S.hist.push(d);restore(d);toast('Redo');}}
function pos(e){const r=cv.getBoundingClientRect();return{x:(e.clientX-r.left)/S.zoom,y:(e.clientY-r.top)/S.zoom};}
function setupStroke(){
  ctx.save();ctx.lineCap='round';ctx.lineJoin='round';
  if(S.tool==='eraser'){ctx.globalCompositeOperation='destination-out';ctx.strokeStyle='rgba(0,0,0,1)';ctx.fillStyle='rgba(0,0,0,1)';ctx.lineWidth=S.size;}
  else if(S.tool==='marker'){ctx.globalCompositeOperation='source-over';ctx.globalAlpha=S.opacity*0.32;ctx.strokeStyle=S.color;ctx.fillStyle=S.color;ctx.lineWidth=S.size*1.8;}
  else{ctx.globalCompositeOperation='source-over';ctx.globalAlpha=S.opacity;ctx.strokeStyle=S.color;ctx.fillStyle=S.color;ctx.lineWidth=S.tool==='pencil'?S.size*0.7:S.size;}
}
function startLaser(x,y){S.laser=[{x,y}];S.laserActive=true;S.laserStop=0;}
function moveLaser(x,y){const last=S.laser[S.laser.length-1];if(last&&Math.hypot(x-last.x,y-last.y)<0.5)return;S.laser.push({x,y});}
function endLaser(){S.laserActive=false;S.laserStop=performance.now();}
function drawLaserPath(){
  octx.beginPath();
  if(S.laser.length===1){const p=S.laser[0];octx.arc(p.x,p.y,octx.lineWidth/2,0,Math.PI*2);octx.fill();return;}
  octx.moveTo(S.laser[0].x,S.laser[0].y);
  for(let i=1;i<S.laser.length-1;i++){
    const cu=S.laser[i],nx=S.laser[i+1];
    const mx=(cu.x+nx.x)/2,my=(cu.y+nx.y)/2;
    octx.quadraticCurveTo(cu.x,cu.y,mx,my);
  }
  const lst=S.laser[S.laser.length-1];octx.lineTo(lst.x,lst.y);
  octx.stroke();
}
function drawLaser(op){
  if(S.laser.length<1)return;
  // DARK + GLOWING laser passes (dark blood-red base, intense glow, bright core)
  const passes=[
    {m:8.0, a:0.22, b:80, c:'120,0,5'},     // very wide dark red halo
    {m:5.5, a:0.45, b:55, c:'180,0,15'},    // dark red glow
    {m:3.5, a:0.75, b:32, c:'220,15,30'},   // deep red
    {m:2.0, a:1.00, b:18, c:'255,40,55'},   // bright red
    {m:0.9, a:1.00, b:12, c:'255,200,200'}, // hot pink core
    {m:0.4, a:1.00, b:6,  c:'255,255,255'}, // white center
  ];
  for(const p of passes){
    octx.save();
    octx.lineCap='round';octx.lineJoin='round';
    octx.shadowColor=`rgba(${p.c},1)`;octx.shadowBlur=p.b;
    octx.strokeStyle=`rgba(${p.c},${p.a*op})`;
    octx.fillStyle=`rgba(${p.c},${p.a*op})`;
    octx.lineWidth=Math.max(1.5,S.size*p.m);
    drawLaserPath();
    octx.restore();
  }
  // Intense glowing tip
  const tip=S.laser[S.laser.length-1];
  octx.save();
  octx.shadowColor='rgba(140,0,10,1)';octx.shadowBlur=70;
  octx.fillStyle=`rgba(180,0,15,${op})`;
  octx.beginPath();octx.arc(tip.x,tip.y,Math.max(7,S.size*3.2),0,Math.PI*2);octx.fill();
  octx.shadowColor='rgba(255,30,40,1)';octx.shadowBlur=40;
  octx.fillStyle=`rgba(255,40,55,${op})`;
  octx.beginPath();octx.arc(tip.x,tip.y,Math.max(4,S.size*1.8),0,Math.PI*2);octx.fill();
  octx.shadowColor='rgba(255,255,255,1)';octx.shadowBlur=25;
  octx.fillStyle=`rgba(255,255,255,${op})`;
  octx.beginPath();octx.arc(tip.x,tip.y,Math.max(2,S.size*0.9),0,Math.PI*2);octx.fill();
  octx.restore();
}
function laserLoop(){
  octx.save();octx.setTransform(1,0,0,1,0,0);octx.clearRect(0,0,ov.width,ov.height);octx.restore();
  if(S.laser.length>0){
    let op=1;
    if(!S.laserActive&&S.laserStop>0){
      const el=performance.now()-S.laserStop;
      if(el>=FADE){S.laser=[];S.laserStop=0;}
      else{const t=el/FADE;op=1-t*t;}
    }
    if(op>0)drawLaser(op);
  }
  requestAnimationFrame(laserLoop);
}
function drawSeg(){
  const n=S.pts.length;if(n<2)return;
  setupStroke();
  if(n===2){
    const p0=S.pts[0],p1=S.pts[1],m={x:(p0.x+p1.x)/2,y:(p0.y+p1.y)/2};
    ctx.beginPath();ctx.moveTo(p0.x,p0.y);ctx.lineTo(m.x,m.y);ctx.stroke();
  } else {
    const p0=S.pts[n-3],p1=S.pts[n-2],p2=S.pts[n-1];
    const m1={x:(p0.x+p1.x)/2,y:(p0.y+p1.y)/2},m2={x:(p1.x+p2.x)/2,y:(p1.y+p2.y)/2};
    ctx.beginPath();ctx.moveTo(m1.x,m1.y);ctx.quadraticCurveTo(p1.x,p1.y,m2.x,m2.y);ctx.stroke();
  }
  ctx.restore();
}
function finishStroke(){
  const n=S.pts.length;if(n<2)return;
  setupStroke();
  const p0=S.pts[n-2],p1=S.pts[n-1],m1={x:(p0.x+p1.x)/2,y:(p0.y+p1.y)/2};
  ctx.beginPath();ctx.moveTo(m1.x,m1.y);ctx.lineTo(p1.x,p1.y);ctx.stroke();
  ctx.restore();
}
function start(e){if(roBool())return;
  if(e.preventDefault)e.preventDefault();
  const{x,y}=pos(e);S.drawing=true;S.sx=x;S.sy=y;
  if(S.tool==='laser'){startLaser(x,y);return;}
  if(S.tool==='text'){
    const t=prompt('Enter text:');
    if(t){ctx.save();ctx.globalAlpha=S.opacity;ctx.fillStyle=S.color;ctx.font=`600 ${Math.max(14,S.size*3)}px -apple-system,sans-serif`;ctx.textBaseline='top';ctx.fillText(t,x,y);ctx.restore();saveHNotify();}
    S.drawing=false;return;
  }
  if(['line','rect','circle','arrow'].includes(S.tool)){S.snap=ctx.getImageData(0,0,cv.width,cv.height);return;}
  S.pts=[{x,y}];
  setupStroke();
  ctx.beginPath();ctx.arc(x,y,ctx.lineWidth/2,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
function move(e){if(roBool())return;
  const{x,y}=pos(e);
  $('dsaSkSP').textContent=`x: ${Math.round(x)} · y: ${Math.round(y)}`;
  if(!S.drawing)return;
  if(S.tool==='laser'){moveLaser(x,y);return;}
  if(['brush','pencil','marker','eraser'].includes(S.tool)){
    const last=S.pts[S.pts.length-1];
    if(last&&Math.hypot(x-last.x,y-last.y)<0.6)return;
    S.pts.push({x,y});drawSeg();return;
  }
  if(S.snap){ctx.save();ctx.setTransform(1,0,0,1,0,0);ctx.putImageData(S.snap,0,0);ctx.restore();shape(S.sx,S.sy,x,y,e.shiftKey);}
}
function end(){if(roBool()){S.drawing=false;return;}
  if(!S.drawing)return;
  if(S.tool==='laser'){endLaser();S.drawing=false;return;}
  const wasShape=S.snap!=null,wasStroke=['brush','pencil','marker','eraser'].includes(S.tool);
  if(wasStroke)finishStroke();
  S.drawing=false;S.snap=null;S.pts=[];
  if(wasShape||wasStroke)saveHNotify();
}
function shape(x1,y1,x2,y2,sh){
  ctx.save();ctx.globalAlpha=S.opacity;ctx.strokeStyle=S.color;ctx.fillStyle=S.color;ctx.lineWidth=S.size;ctx.lineCap='round';ctx.lineJoin='round';
  if(S.tool==='line'){ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke();}
  else if(S.tool==='rect'){
    let w=x2-x1,h=y2-y1;
    if(sh){const s=Math.min(Math.abs(w),Math.abs(h));w=Math.sign(w)*s;h=Math.sign(h)*s;}
    const r=Math.min(10,Math.min(Math.abs(w),Math.abs(h))/4);
    ctx.beginPath();if(ctx.roundRect)ctx.roundRect(x1,y1,w,h,r);else ctx.rect(x1,y1,w,h);ctx.stroke();
  } else if(S.tool==='circle'){
    const dx=x2-x1,dy=y2-y1;ctx.beginPath();
    if(sh){const r=Math.min(Math.abs(dx),Math.abs(dy));ctx.arc(x1,y1,r,0,Math.PI*2);}
    else ctx.ellipse((x1+x2)/2,(y1+y2)/2,Math.abs(dx)/2,Math.abs(dy)/2,0,0,Math.PI*2);
    ctx.stroke();
  } else if(S.tool==='arrow'){
    const hd=Math.max(12,S.size*2.5),an=Math.atan2(y2-y1,x2-x1);
    ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke();
    ctx.beginPath();ctx.moveTo(x2,y2);
    ctx.lineTo(x2-hd*Math.cos(an-Math.PI/6),y2-hd*Math.sin(an-Math.PI/6));
    ctx.lineTo(x2-hd*Math.cos(an+Math.PI/6),y2-hd*Math.sin(an+Math.PI/6));
    ctx.closePath();ctx.fill();
  }
  ctx.restore();
}
let toastT;
function toast(m){const t=$('dsaSkToast');$('dsaSkTT').textContent=m;t.classList.add('show');clearTimeout(toastT);toastT=setTimeout(()=>t.classList.remove('show'),1400);}
cv.addEventListener('pointerdown',e=>{cv.setPointerCapture(e.pointerId);start(e);});
cv.addEventListener('pointermove',move);
cv.addEventListener('pointerup',end);
cv.addEventListener('pointercancel',end);
function setTool(n){
  const b=mount.querySelector(`[data-tool="${n}"]`);if(!b)return;
  mount.querySelectorAll('[data-tool]').forEach(x=>x.classList.remove('active'));
  b.classList.add('active');S.tool=n;
  $('dsaSkST').textContent=n.charAt(0).toUpperCase()+n.slice(1);
  cv.style.cursor=n==='text'?'text':'crosshair';
}
mount.querySelectorAll('[data-tool]').forEach(b=>b.addEventListener('click',()=>setTool(b.dataset.tool)));
$('dsaSkColors').addEventListener('click',e=>{
  const sw=e.target.closest('.swatch');if(!sw)return;
  mount.querySelectorAll('#dsaSkColors .swatch').forEach(s=>s.classList.remove('active'));
  sw.classList.add('active');setColor(sw.dataset.color);
});
$('dsaSkCp').addEventListener('input',e=>{
  mount.querySelectorAll('#dsaSkColors .swatch').forEach(s=>s.classList.remove('active'));
  setColor(e.target.value);
});
function setColor(c){S.color=c;$('dsaSkSCT').textContent=c;$('dsaSkSCD').style.background=c;}
const sR=$('dsaSkSizeR'),oR=$('dsaSkOpR');
function upR(el){const mn=+el.min||0,mx=+el.max||100;el.style.setProperty('--val',(((+el.value-mn)/(mx-mn))*100)+'%');}
sR.addEventListener('input',e=>{S.size=+e.target.value;$('dsaSkSizeV').textContent=S.size;$('dsaSkSS').textContent=S.size+' px';upR(sR);});
oR.addEventListener('input',e=>{S.opacity=+e.target.value/100;$('dsaSkOpV').textContent=e.target.value;upR(oR);});
upR(sR);upR(oR);
$('dsaSkUndoBtn').addEventListener('click',undo);
$('dsaSkRedoBtn').addEventListener('click',redoFn);
$('dsaSkClearBtn').addEventListener('click',()=>{
  if(confirm('Clear canvas?')){ctx.save();ctx.setTransform(1,0,0,1,0,0);ctx.clearRect(0,0,cv.width,cv.height);ctx.restore();saveH();syncInk();if(typeof hooks.afterClear==='function')hooks.afterClear();onChange();toast('Cleared');}
});
$('dsaSkSaveBtn').addEventListener('click',()=>{
  const t=document.createElement('canvas');t.width=cv.width;t.height=cv.height;
  const tc=t.getContext('2d');tc.fillStyle=isDark()?'#2c2c2e':'#fff';tc.fillRect(0,0,t.width,t.height);
  tc.drawImage(cv,0,0);
  const a=document.createElement('a');a.download=`sketch-${Date.now()}.png`;a.href=t.toDataURL('image/png');a.click();toast('Saved!');
});

$('dsaSkZiBtn').addEventListener('click',()=>{S.zoom=Math.min(S.zoom+0.25,5);applyZoom();});
$('dsaSkZoBtn').addEventListener('click',()=>{S.zoom=Math.max(S.zoom-0.25,1);applyZoom();});
$('dsaSkZrBtn').addEventListener('click',()=>{S.zoom=1;applyZoom();});
cw.addEventListener('wheel',e=>{if(e.ctrlKey||e.metaKey){e.preventDefault();S.zoom=Math.max(1,Math.min(5,S.zoom+(e.deltaY<0?0.1:-0.1)));applyZoom();}},{passive:false});
$('dsaSkImgInput').addEventListener('change',e=>{
  const f=e.target.files[0];if(!f)return;
  const r=new FileReader();r.onload=ev=>{const i=new Image();i.onload=()=>{
    const cssW=cv.width/DPR,cssH=cv.height/DPR;
    const rt=Math.min(cssW/i.width,cssH/i.height)*0.9;
    const w=i.width*rt,h=i.height*rt;
    ctx.drawImage(i,(cssW-w)/2,(cssH-h)/2,w,h);saveHNotify();toast('Imported');
  };i.src=ev.target.result;};r.readAsDataURL(f);e.target.value='';
});
$('dsaSkThemeBtn').addEventListener('click',()=>{mount.dataset.theme=isDark()?'light':'dark';toast(isDark()?'Dark mode':'Light mode');});
function onDocKeyInner(e){
  if(['INPUT','TEXTAREA'].includes(e.target.tagName))return;
  const k=e.key.toLowerCase();
  if((e.ctrlKey||e.metaKey)&&k==='z'&&!e.shiftKey){e.preventDefault();undo();}
  else if((e.ctrlKey||e.metaKey)&&(k==='y'||(e.shiftKey&&k==='z'))){e.preventDefault();redoFn();}
  else if((e.ctrlKey||e.metaKey)&&k==='s'){e.preventDefault();$('dsaSkSaveBtn').click();}
  else if(k==='b')setTool('brush');else if(k==='p')setTool('pencil');
  else if(k==='h')setTool('marker');else if(k==='e')setTool('eraser');
  else if(k==='q')setTool('laser');else if(k==='l')setTool('line');
  else if(k==='r')setTool('rect');else if(k==='c')setTool('circle');
  else if(k==='a')setTool('arrow');else if(k==='t')setTool('text');
  else if(k==='+'||k==='=')$('dsaSkZiBtn').click();
  else if(k==='-'||k==='_')$('dsaSkZoBtn').click();
  else if(k==='0')$('dsaSkZrBtn').click();
  else if(k==='f')$('dsaSkFsBtn').click();
}
let rt;function onWinResize(){clearTimeout(rt);rt=setTimeout(resize,100);}
window.addEventListener('resize',onWinResize);



    $('dsaSkFsBtn').addEventListener('click', () => {
        if (isFakeFs) leaveFakeFullscreen();
        else enterFakeFullscreen();
    });
    const backBtn = $('dsaSkBackBtn');
    if (backBtn) backBtn.addEventListener('click', () => leaveFakeFullscreen());

    function onDocKey(e) {
        if (e.key === 'Escape' && isFakeFs) {
            e.preventDefault();
            e.stopPropagation();
            leaveFakeFullscreen();
            return;
        }
        onDocKeyInner(e);
    }
    document.addEventListener('keydown', onDocKey, true);

    const ro = new ResizeObserver(() => {
        clearTimeout(rt);
        rt = setTimeout(resize, 50);
    });
    ro.observe(cw);

    resize();
    saveH();
    syncInk();
    onChange();
    laserLoop();

    const api = {
        clear() {
            ctx.save();
            ctx.setTransform(1, 0, 0, 1, 0, 0);
            ctx.clearRect(0, 0, cv.width, cv.height);
            ctx.restore();
            S.hist = [];
            saveH();
            hasInk = false;
            if (typeof hooks.afterClear === 'function') hooks.afterClear();
            onChange();
        },
        zoomIn() {
            S.zoom = Math.min(S.zoom + 0.25, 5);
            applyZoom();
        },
        zoomOut() {
            S.zoom = Math.max(S.zoom - 0.25, 1);
            applyZoom();
        },
        resetZoom() {
            S.zoom = 1;
            applyZoom();
        },
        loadDataUrl(url) {
            if (!url || !String(url).trim()) {
                api.clear();
                return;
            }
            const im = new Image();
            im.onload = () => {
                const cssW = cv.width / DPR;
                const cssH = cv.height / DPR;
                const rt0 = Math.min(cssW / im.width, cssH / im.height) * 0.9;
                const w = im.width * rt0;
                const h = im.height * rt0;
                ctx.save();
                ctx.setTransform(DPR, 0, 0, DPR, 0, 0);
                ctx.clearRect(0, 0, cssW, cssH);
                ctx.restore();
                ctx.drawImage(im, (cssW - w) / 2, (cssH - h) / 2, w, h);
                S.hist = [];
                saveH();
                syncInk();
                onChange();
            };
            im.onerror = () => api.clear();
            im.src = String(url).trim();
        },
        toDataUrl() {
            try {
                return cv.toDataURL('image/png');
            } catch (_) {
                return '';
            }
        },
        toPersistedSketchDataUrl() {
            try {
                return cv.toDataURL('image/jpeg', 0.82);
            } catch (_) {
                try {
                    return cv.toDataURL('image/png');
                } catch (_) {
                    return '';
                }
            }
        },
        getHasInk() {
            return hasInk;
        },
        syncHasInkFromPixels() {
            syncInk();
        },
        exitFullscreen() {
            leaveFakeFullscreen();
        },
        isFullscreen() {
            return isFakeFs;
        },
        destroy() {
            if (destroyed) return;
            destroyed = true;
            leaveFakeFullscreen();
            try {
                ro.disconnect();
            } catch (_) {}
            document.removeEventListener('keydown', onDocKey, true);
            window.removeEventListener('resize', onWinResize);
            editorRoot.innerHTML = '';
            editorRoot.classList.remove('dsa-sketch-studio-host', 'dsa-sketch-studio-host--fullscreen');
        },
    };
    return api;
}

function dsaWireSketchEditorStudioStub() {
    return {
        clear() {},
        zoomIn() {},
        zoomOut() {},
        resetZoom() {},
        loadDataUrl() {},
        toDataUrl() {
            return '';
        },
        toPersistedSketchDataUrl() {
            return '';
        },
        getHasInk() {
            return false;
        },
        syncHasInkFromPixels() {},
        exitFullscreen() {},
        isFullscreen() {
            return false;
        },
        destroy() {},
    };
}
